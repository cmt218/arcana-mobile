<?xml version="1.0" encoding="utf-8"?>
<!-- qa-only: permits cleartext to the emulator's host loopback so a minified,
     non-debuggable build can be driven against a local dev server. The shipping
     release variant does not merge this. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config_qa" />
</manifest>
