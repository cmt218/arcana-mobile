// Optional include of gitignored analytics/observability keys (PostHog, Sentry).
// Absent on a fresh clone → the $(…) refs in Info.plist resolve empty → SDKs off.
#include? "Secrets.xcconfig"

TEAM_ID=J3FNK8L4G2

PRODUCT_NAME=Arcana
PRODUCT_BUNDLE_IDENTIFIER=org.arcana.mobile

CURRENT_PROJECT_VERSION=15
MARKETING_VERSION=1.0.4
